import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Banaan here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */

public class Banaan extends Actor
{
    /**
     * Act - do whatever the Banaan wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */

    //Deze code was origineel bestemd voor het schieten. Het plan was om met een banaaan en met een pizza te schieten
    //maar helaas brak de banaan heel mijn spel en het heeft zelfs mijn computer gecrasht toen ik aan het debuggen was. Heel het scherm was groen
    // gelukkig heb ik met wat hulp van een vriend de pizza functie zo omgebouwd dat hij zowel links als rechts kan schieten en de banaan heeft vervangen. R.I.P Bananen Gun
   // }
    public void act()
    {
        //Bewegen();
        //setRotation(180);
        //abortus();
    //}

    //public void Bewegen()
    //{
        //move(16);
    //}
    //public void abortus()
    //{
       // if(getX() >= getWorld().getWidth()-1)
         // getWorld().removeObject(this); // als de bananen de zijkant van de wereld aanraken, zijn ze gone
//        else  if(getX() < 1)
          //getWorld().removeObject(this); // zelfde als 2 zinnen erboven
        //else  if(getY() >= getWorld().getHeight()-1)
          //getWorld().removeObject(this);
        //else  if(getY() < 1)                   // als hij op de grond komt zal hij ook verdwijnen
          //getWorld().removeObject(this);       
        //else if (isTouching(grond.class))  // als het de grond aanraakt zal hij ook verdwijnen
        //getWorld().removeObject(this);
    }
}


